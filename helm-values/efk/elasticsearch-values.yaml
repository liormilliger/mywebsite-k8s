# A single-node cluster for development/portfolio purposes
replicas: 1
minimumMasterNodes: 1

# Configure persistent storage using the 'gp3' StorageClass
volumeClaimTemplate:
  storageClassName: gp2
  resources:
    requests:
      storage: 15Gi
